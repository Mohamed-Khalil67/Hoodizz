# PostgreSQL connection string (Prisma)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/retailmarkt"

# Stripe — get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET="sk_test_xxx"
# Get this from Stripe CLI (`stripe listen`) or Dashboard > Webhooks
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# Frontend origin (used for Stripe success/cancel redirects + CORS)
FRONTEND_URL="http://localhost:4200"

# Optional: NODE_ENV controls GraphQL playground/introspection
# NODE_ENV=development
